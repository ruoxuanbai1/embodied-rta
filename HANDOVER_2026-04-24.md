# 交接记录 2026-04-24

## 📊 当前状态
v6 重新分析运行中 (PID 565808)

## 🔍 关键发现
1. **QPOS_LIMITS 错误** - v3 用了错误的限位定义，导致 R1 计算全错
2. **GT 定义太宽松** - 所有步骤都是 danger=True（连 normal 场景）
3. **文件太大** - 每个 ~5GB，需要流式处理

## 📋 明日检查清单
- [ ] 检查 v6 结果 (`/mnt/data/ablation_experiments/reanalysis_v6_result.json`)
- [ ] 如果结果仍然 100%/0%，需要重新定义 GT
- [ ] 如果结果合理，运行阈值网格搜索

## 🔧 命令参考
```bash
# 检查进度
ssh root@js4.blockelite.cn -p 10320
tail -20 /root/act/reanalyze_v6.log

# 查看结果
cat /mnt/data/ablation_experiments/reanalysis_v6_result.json

# 如果 v6 完成，检查 R1/R2/R3 分布
python3 -c '
import json
with open("/mnt/data/ablation_experiments/reanalysis_v6_result.json") as f:
    r = json.load(f)
print("Full:", r["result"]["full"])
print("R1:", r["result"]["r1"])
print("R2:", r["result"]["r2"])
print("R3:", r["result"]["r3"])
'
```

## ⚠️ 注意事项
- 不要使用预存的 alarm 字段
- QPOS_LIMITS 必须与 rta_v5_runner.py 一致
- 如果 GT 还是 100% danger，需要重新定义 GT
